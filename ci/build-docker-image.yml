platform: linux

image_resource:
  type: registry-image
  source:
    repository: concourse/oci-build-task
    tag: master

inputs:
  - name: src

outputs:
  - name: image

caches:
  - path: cache

run:
  path: build

params:
  CONTEXT: src
